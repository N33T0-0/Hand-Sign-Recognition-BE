{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "start":{
        "startCommand": "pip install packages.txt"
    },
    "deploy": {
      "startCommand": "uvicorn main:app --bind \"[::]:8100\""
    }
  }